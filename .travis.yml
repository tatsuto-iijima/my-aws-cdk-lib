language: node_js

node_js:
  - lts/*

branches:
  only:
  - master
  - develop
  - /^release\/.*/

install:
  - npm install -g typescript
  - npm install -g aws-cdk
  - cd ./packages/lambda-nodejs
  - npm install

script:
  - npm test